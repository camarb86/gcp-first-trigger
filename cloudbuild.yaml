steps:
- name: 'alpine'
  entrypoint: 'sh'
  args:
  - '-c'
  - 'echo "The cloud build triggered successfully !!!"; echo "Repository: $REPO_NAME"; echo "Branch: $BRANCH_NAME"; echo "Commit ID: $COMMIT_SHA"'

#options:
#  logging: CLOUD_LOGGING_ONLY
#logsBucket: gs://gcp-devops-course-bucket-amar

logsBucket: 'gs://gcp-devops-course-bucket-amar'

options:
  logging: GCS_ONLY
